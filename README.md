# TMDB CLI (tmdb-app)

Simple command line tool to fetch movies from The Movie Database (TMDB).

## Setup

1. Clone or download this project
2. Install dependencies:

```bash
npm install

https://github.com/ekta-developer/TMDB_CLI_tool
